Hardware compare outputs land here (`SUMMARY.txt`, JSON, CLAIM_GATE).

Generated only on hosts with ≥2 NUMA nodes via:

```bash
./examples/compare_numa.sh
```

On 1-node hosts the script exits 2 and does not invent results.
